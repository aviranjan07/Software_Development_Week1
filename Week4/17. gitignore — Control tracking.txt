🧠 17..gitignore — Control tracking

👉 File to ignore:

node_modules/
.env
dist/

## 🚫 `.gitignore` — Control Tracking (Deep + Practical)

---

# 🧠 Core Idea

> **`.gitignore` = tell Git which files/folders it should NOT track**

👉 Prevents unwanted files from being added to your repository

---

# 🤔 WHY we use `.gitignore`

### ✅ 1. Avoid unnecessary files

* Temporary files
* Logs
* Build outputs

---

### ✅ 2. Protect sensitive data 🔐

* API keys
* Passwords
* `.env` files

---

### ✅ 3. Keep repo clean

👉 Only important code is tracked

---

# 📂 Real Examples

Common files you should ignore:

* `node_modules/`
* `.env`
* `.log` files
* `dist/` or `build/`
* OS files (`.DS_Store`, `Thumbs.db`)

---

# ⚙️ HOW to use `.gitignore`

---

## ✅ Step 1: Create file

```bash id="tn0jr3"
touch .gitignore
```

---

## ✅ Step 2: Add rules

Example:

```gitignore id="ym6s3c"
# node modules
node_modules/

# environment file
.env

# logs
*.log

# build folder
dist/

# OS files
.DS_Store
```

---

## 📊 Visual Understanding

```text id="p1fscm"
Project Folder:

✔ app.js        → tracked
✔ style.css     → tracked
❌ node_modules → ignored
❌ .env         → ignored
```

---

# 🔍 Pattern Rules (IMPORTANT)

---

## 🔹 Ignore folder

```gitignore id="zg04vr"
node_modules/
```

---

## 🔹 Ignore file type

```gitignore id="e55bc7"
*.log
```

---

## 🔹 Ignore specific file

```gitignore id="bgs5bc"
secret.txt
```

---

## 🔹 Ignore all files in folder

```gitignore id="q7x7nl"
build/*
```

---

## 🔹 Except a file (!)

```gitignore id="7h8o8l"
*.log
!important.log
```

👉 Ignore all logs EXCEPT one

---

# ⚠️ Important Rule

👉 `.gitignore` works only for **untracked files**

---

## ❌ If file already tracked:

```bash id="36rjzn"
git rm --cached file.js
```

Then it will be ignored

---

# 🧩 Real Workflow Example

```bash id="2rdkhl"
# create ignore file
touch .gitignore

# add rules
echo "node_modules/" >> .gitignore

# remove tracked files
git rm -r --cached node_modules

# commit
git add .
git commit -m "Add .gitignore"
```

---

# 🏆 Pro Tips

---

### 1️⃣ Use global `.gitignore`

```bash id="ybx4bw"
git config --global core.excludesfile ~/.gitignore_global
```

👉 Ignore files across all projects

---

### 2️⃣ Use templates

GitHub provides ready-made templates:

* Node.js
* Python
* Java

---

### 3️⃣ Keep `.env` always ignored 🔐

👉 Never push secrets

---

# ⚠️ Common Mistakes

---

### ❌ Adding `.gitignore` too late

👉 Files already tracked

---

### ❌ Forgetting to ignore large folders

👉 Repo becomes slow

---

### ❌ Ignoring important files

👉 Breaks project

---

# 🧠 Mental Model

👉 Think:

* Git = security guard
* `.gitignore` = “Do not enter” list 🚫

---

# 📌 Final Summary

✔ Controls what Git tracks
✔ Keeps repo clean & secure
✔ Uses simple pattern rules
✔ Works only on untracked files

---

## 🧠 One-line understanding

> 🚫 **`.gitignore` = “Git, ignore these files completely”**

---
