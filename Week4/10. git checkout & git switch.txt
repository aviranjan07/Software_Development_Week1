🧠 10.git checkout / git switch

git checkout feature-login

💡 Deep Understanding:

👉 Moves HEAD to another branch

Modern way:
git switch feature-login
Create + switch:
git checkout -b feature-login



## 🔄 `git checkout` vs `git switch` — Clear & Deep Understanding

---

# 🧠 Core Idea

👉 Both are used to **move between branches or commits**

But:

* `git checkout` = 🔥 old, multi-purpose (does many things)
* `git switch` = ✅ modern, safer (only for branches)

---

# ⚖️ Quick Comparison

| Feature           | `git checkout`               | `git switch`          |
| ----------------- | ---------------------------- | --------------------- |
| Introduced        | Old (classic Git)            | New (Git 2.23+)       |
| Purpose           | Multiple uses                | Only branch switching |
| Risk              | Higher (can overwrite files) | Safer                 |
| Beginner-friendly | ❌ Confusing                  | ✅ Easy                |

---

# 🧭 1. `git checkout` (Old Powerful Command)

## ✅ What it can do:

### 🔹 Switch branch

```bash
git checkout main
```

---

### 🔹 Create + switch branch

```bash
git checkout -b feature/login
```

---

### 🔹 Checkout a commit (detached HEAD)

```bash
git checkout abc123
```

---

### 🔹 Restore file

```bash
git checkout file.js
```

👉 ⚠️ This is dangerous (can overwrite changes)

---

## 🧠 Problem with `checkout`

👉 One command = too many roles
👉 Confusing for beginners

---

# 🌿 2. `git switch` (Modern & Clean)

👉 Introduced to **simplify Git**

---

## ✅ What it does:

### 🔹 Switch branch

```bash
git switch main
```

---

### 🔹 Create + switch

```bash
git switch -c feature/login
```

---

### 🔹 Switch to previous branch

```bash
git switch -
```

---

## ❌ What it does NOT do:

* Cannot restore files
* Cannot checkout commits (by default)

👉 That’s intentional (safer)

---

# 🔥 Visual Understanding

## Switching branches

```text
Before:

main → A ─── B ─── C
                 ↑
               HEAD

feature → A ─── B
```

---

### After:

```bash
git switch feature
```

```text
HEAD → feature → B
```

👉 You moved to another timeline

---

# ⚠️ Detached HEAD (Important)

```bash
git checkout abc123
```

```text
HEAD → commit (no branch)
```

👉 You are NOT on any branch
👉 Changes can be lost if not careful

---

# 🚀 Best Practice (Modern Way)

👉 Use:

```bash
git switch
```

👉 Instead of:

```bash
git checkout
```

---

# 🧩 Real Workflow Example

```bash
git switch main
git pull

git switch -c feature/navbar
# work
git add .
git commit -m "Add navbar"

git switch main
git merge feature/navbar
```

---

# 🧠 Mental Model

👉 Think like:

* `checkout` = 🧰 Swiss army knife (many tools, confusing)
* `switch` = 🎯 One job, done well

---

# 🏆 Pro Tips

### 1️⃣ Use modern commands

```bash
git switch
git restore
```

👉 Instead of checkout

---

### 2️⃣ Check branch before switching

```bash
git branch
```

---

### 3️⃣ Avoid losing work

If changes exist:

```bash
git stash
git switch branch-name
```

---

# 📌 Final Summary

👉 `git checkout`:

* Old
* Powerful but confusing

👉 `git switch`:

* New
* Clean and safe
* Recommended ✅

---

## 🧠 One-line understanding

> 🔄 `git switch` = change branch safely
> 🧰 `git checkout` = old multi-purpose command

---