# 🔹 2. Loop Control Statements — Deep Understanding

Loop control statements **control how loops behave while running**.
Sometimes we need to **stop a loop early**, **skip some iterations**, or **do nothing temporarily**.

These statements help programmers **avoid infinite loops and control program flow**.

Main loop control statements:

```
break
continue
pass (language specific)
```

Used inside loops like:

```
for loop
while loop
do-while loop
```

---

# 1️⃣ break Statement

`break` **immediately stops the loop** and exits it.

When the program encounters `break`, the loop **terminates instantly**, and execution moves to the **next line after the loop**.

### Syntax

```java
break;
```

---

## Example 1 — Simple break

```java
for(int i = 1; i <= 10; i++){

    if(i == 5){
        break;
    }

    System.out.println(i);
}
```

### Output

```
1
2
3
4
```

### Explanation

Loop starts from **1 to 10**

When `i == 5`

```
break executes
loop stops immediately
```

So **5 to 10 never execute**.

---

## Real Example — Password Attempts

```java
int attempts = 1;

while(attempts <= 3){

    System.out.println("Checking password");

    if(attempts == 2){
        System.out.println("Login successful");
        break;
    }

    attempts++;
}
```

### Output

```
Checking password
Checking password
Login successful
```

The loop stops after **successful login**.

---

# 2️⃣ continue Statement

`continue` **skips the current iteration** and moves to the **next loop iteration**.

The loop **does NOT stop**, it just **skips that one step**.

### Syntax

```java
continue;
```

---

## Example — Skip number 5

```java
for(int i = 1; i <= 10; i++){

    if(i == 5){
        continue;
    }

    System.out.println(i);
}
```

### Output

```
1
2
3
4
6
7
8
9
10
```

Explanation:

```
When i = 5
continue runs
System.out.println skipped
Loop continues with i = 6
```

---

## Real Example — Skip Failed Data

Imagine processing student marks.

```java
for(int i = 1; i <= 5; i++){

    if(i == 3){
        continue;
    }

    System.out.println("Processing student " + i);
}
```

### Output

```
Processing student 1
Processing student 2
Processing student 4
Processing student 5
```

Student **3 skipped**.

---

# 3️⃣ pass Statement (Language Specific)

`pass` means **do nothing**.

It is mainly used in **Python**, not Java.

Example in Python:

```python
for i in range(5):
    if i == 3:
        pass
    print(i)
```

Output

```
0
1
2
3
4
```

Explanation:

```
pass → placeholder
program continues normally
```

Developers use `pass` when **logic will be added later**.

Example:

```python
if condition:
    pass
```

This prevents **syntax error when block is empty**.

Java does **not require `pass`**.

---

# 🔹 Difference Between break and continue

| Feature         | break     | continue       |
| --------------- | --------- | -------------- |
| Stops loop      | Yes       | No             |
| Skips iteration | No        | Yes            |
| Loop continues  | No        | Yes            |
| Use case        | Exit loop | Skip condition |

---

# Example Comparison

### break example

```java
for(int i = 1; i <= 5; i++){

    if(i == 3){
        break;
    }

    System.out.println(i);
}
```

Output

```
1
2
```

Loop stops completely.

---

### continue example

```java
for(int i = 1; i <= 5; i++){

    if(i == 3){
        continue;
    }

    System.out.println(i);
}
```

Output

```
1
2
4
5
```

Loop continues but **skips 3**.

---

# Visual Flow

### break

```
Loop Start
   ↓
Condition met
   ↓
break
   ↓
Loop Ends
```

---

### continue

```
Loop Start
   ↓
Condition met
   ↓
continue
   ↓
Skip iteration
   ↓
Next loop cycle
```

---

# Real Programming Uses

### 1️⃣ Search System

Stop loop when item found.

```java
if(itemFound){
    break;
}
```

---

### 2️⃣ Skip Invalid Data

```java
if(dataInvalid){
    continue;
}
```

---

### 3️⃣ Game Development

Stop loop when player loses.

```
if(playerDead){
    break;
}
```

---

### 4️⃣ Login Attempts

Stop after successful login.

```
if(loginSuccess){
    break;
}
```

---

# Infinite Loop Problem

Sometimes loops run **forever**.

Example:

```java
while(true){
    System.out.println("Running");
}
```

To stop it:

```java
while(true){

    if(condition){
        break;
    }
}
```

This prevents **infinite loops**.

---

# Common Beginner Mistakes

### 1️⃣ Using break accidentally

```java
if(i == 2){
    break;
}
```

Stops the whole loop unexpectedly.

---

### 2️⃣ Forgetting continue behavior

People think it stops the loop.

But it **only skips one iteration**.

---

### 3️⃣ Infinite loops

Example:

```java
while(i < 10){
}
```

Missing:

```
i++
```

Loop never ends.

---

# Debugging Tip

Print loop values.

```java
System.out.println("i = " + i);
```

This helps detect **where loop stops or skips**.

---

# Practice Problems

### Problem 1

Print numbers **1 to 20**, but skip **multiples of 3**.

---

### Problem 2

Print numbers **1 to 50**, stop when number **25 appears**.

---

### Problem 3

Find first number divisible by **7 between 1 and 100**.

Use `break`.

---

### Problem 4

Print numbers **1 to 10**, skip **even numbers**.

---

# Real Interview Question

Print numbers **1–100**, but:

```
Skip numbers divisible by 5
Stop loop if number = 73
```

This tests:

```
continue
break
conditions
loops
```

---

# Why Loop Control Statements Are Important

Used in:

| System          | Usage                       |
| --------------- | --------------------------- |
| Search engines  | Stop after result found     |
| Login systems   | Stop after correct password |
| Data processing | Skip corrupted data         |
| Games           | Stop when game over         |
| AI loops        | Control training iterations |

Without loop control statements, programs **lose efficiency and control**.

---

✅ If you want, next I can also teach **EXTREMELY important Week-2 topics** that most beginners miss:
